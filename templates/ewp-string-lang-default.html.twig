{#
/**
 * @file
 * Default theme implementation of a String with Optional Lang element.
 *
 * Available variables:
 * - string: the string component.
 * - langcode: the language code.
 * - langname: the language name.
 *
 * @see template_preprocess()
 *
 * @ingroup themeable
 */
#}
{{ string }}{% if langcode %}&nbsp;<small><abbr title="{{ langname }}">{{ langcode }}</abbr></small>{% endif %}
